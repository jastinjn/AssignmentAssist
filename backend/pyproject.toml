[project]
name = "backend"
version = "0.1.0"
description = "Agentic teacher chatbot backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "openai-agents>=0.0.15",
    "prisma>=0.15",
    "pydantic-settings>=2.0",
    "python-dotenv>=1.0",
]
